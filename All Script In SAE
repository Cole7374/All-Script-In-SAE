-- ================= RENE BATERBONIA HUB - STEAL AN EGG =================
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer
local ProtectedGui = (gethui and gethui()) or CoreGui

if ProtectedGui:FindFirstChild("ReneBaterboniaMainGui") then 
    ProtectedGui.ReneBaterboniaMainGui:Destroy() 
end
if ProtectedGui:FindFirstChild("ReneBaterboniaToggleGui") then 
    ProtectedGui.ReneBaterboniaToggleGui:Destroy() 
end

-- ================= AUTO PLAY MUSIC (Walay Loop) =================
pcall(function()
    local StartupSound = Instance.new("Sound")
    StartupSound.SoundId = "rbxassetid://113377354243758"
    StartupSound.Volume = 1.0
    StartupSound.Looped = false
    StartupSound.Parent = ProtectedGui
    StartupSound:Play()
    _G.CurrentCustomMusic = StartupSound
end)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ReneBaterboniaMainGui"
ScreenGui.Parent = ProtectedGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.IgnoreGuiInset = true

local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "ReneBaterboniaToggleGui"
ToggleGui.Parent = ProtectedGui
ToggleGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ToggleGui.IgnoreGuiInset = true

-- ================= SMOOTH DRAG SYSTEM (Walay Bug) =================
local function MakeDraggable(guiObject)
    local dragging, dragInput, dragStart, startPos
    
    guiObject.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = guiObject.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    
    guiObject.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            guiObject.Position = UDim2.new(
                startPos.X.Scale, 
                startPos.X.Offset + delta.X, 
                startPos.Y.Scale, 
                startPos.Y.Offset + delta.Y
            )
        end
    end)
end

-- ================= FLOATING TOGGLE ICON =================
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Parent = ToggleGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(15, 30, 55)
ToggleButton.BackgroundTransparency = 0.3
ToggleButton.Position = UDim2.new(0.02, 0, 0.4, 0)
ToggleButton.Size = UDim2.new(0, 44, 0, 44)
ToggleButton.Image = "rbxassetid://108764707174374"
ToggleButton.AutoButtonColor = true

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(1, 0)
ToggleCorner.Parent = ToggleButton

local ToggleStroke = Instance.new("UIStroke")
ToggleStroke.Color = Color3.fromRGB(0, 180, 255)
ToggleStroke.Transparency = 0.1
ToggleStroke.Thickness = 2.5
ToggleStroke.Parent = ToggleButton

MakeDraggable(ToggleButton)

-- ================= MAIN WINDOW =================
local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 20, 38)
MainFrame.BackgroundTransparency = 0.2
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.Size = UDim2.new(0, 520, 0, 310)

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(0, 160, 255)
MainStroke.Transparency = 0.15
MainStroke.Thickness = 2
MainStroke.Parent = MainFrame

MakeDraggable(MainFrame)

-- Top Bar
local TopBar = Instance.new("Frame")
TopBar.Parent = MainFrame
TopBar.BackgroundTransparency = 1
TopBar.Size = UDim2.new(1, 0, 0, 35)

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Parent = TopBar
TitleLabel.BackgroundTransparency = 1
TitleLabel.Position = UDim2.new(0, 12, 0, 0)
TitleLabel.Size = UDim2.new(0, 380, 1, 0)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "RENE BATERBONIA HUB - Steal An Egg"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 13
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

local CloseButton = Instance.new("TextButton")
CloseButton.Parent = TopBar
CloseButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseButton.BackgroundTransparency = 0.3
CloseButton.Position = UDim2.new(1, -30, 0.5, -10)
CloseButton.Size = UDim2.new(0, 20, 0, 20)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 11

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(1, 0)
CloseCorner.Parent = CloseButton

CloseButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- ================= SIDEBAR NAVIGATION =================
local Sidebar = Instance.new("ScrollingFrame")
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Color3.fromRGB(15, 28, 50)
Sidebar.BackgroundTransparency = 0.4
Sidebar.Position = UDim2.new(0, 8, 0, 38)
Sidebar.Size = UDim2.new(0, 125, 0, 260)
Sidebar.CanvasSize = UDim2.new(0, 0, 2.0, 0)
Sidebar.ScrollBarThickness = 2
Sidebar.BorderSizePixel = 0

local SidebarCorner = Instance.new("UICorner")
SidebarCorner.CornerRadius = UDim.new(0, 8)
SidebarCorner.Parent = Sidebar

local SidebarLayout = Instance.new("UIListLayout")
SidebarLayout.Parent = Sidebar
SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
SidebarLayout.Padding = UDim.new(0, 4)

-- ================= CONTENT AREA =================
local ContentArea = Instance.new("Frame")
ContentArea.Parent = MainFrame
ContentArea.BackgroundTransparency = 1
ContentArea.Position = UDim2.new(0, 140, 0, 38)
ContentArea.Size = UDim2.new(0, 370, 0, 260)

local tabsList = {}
local activeTab = nil

local function CreateTab(name)
    local TabButton = Instance.new("TextButton")
    TabButton.Parent = Sidebar
    TabButton.BackgroundColor3 = Color3.fromRGB(18, 40, 70)
    TabButton.BackgroundTransparency = 0.5
    TabButton.Size = UDim2.new(0, 115, 0, 28)
    TabButton.Font = Enum.Font.GothamMedium
    TabButton.Text = name
    TabButton.TextColor3 = Color3.fromRGB(220, 235, 255)
    TabButton.TextSize = 11

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 6)
    BtnCorner.Parent = TabButton

    local TabPage = Instance.new("ScrollingFrame")
    TabPage.Parent = ContentArea
    TabPage.BackgroundTransparency = 1
    TabPage.Size = UDim2.new(1, 0, 1, 0)
    TabPage.CanvasSize = UDim2.new(0, 0, 1.8, 0)
    TabPage.ScrollBarThickness = 3
    TabPage.Visible = false
    TabPage.BorderSizePixel = 0

    local PageLayout = Instance.new("UIListLayout")
    PageLayout.Parent = TabPage
    PageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    PageLayout.Padding = UDim.new(0, 6)

    TabButton.MouseButton1Click:Connect(function()
        for _, page in pairs(tabsList) do
            page.Visible = false
        end
        TabPage.Visible = true
        
        pcall(function()
            local ClickSound = Instance.new("Sound")
            ClickSound.SoundId = "rbxassetid://12221967"
            ClickSound.Volume = 1.0
            ClickSound.Parent = ProtectedGui
            ClickSound:Play()
        end)
    end)

    if not activeTab then
        TabPage.Visible = true
        activeTab = TabPage
    end

    table.insert(tabsList, TabPage)
    return TabPage
end

local function AddParagraph(tab, title, content)
    local Frame = Instance.new("Frame")
    Frame.Parent = tab
    Frame.BackgroundColor3 = Color3.fromRGB(18, 35, 60)
    Frame.BackgroundTransparency = 0.3
    Frame.Size = UDim2.new(0, 355, 0, 50)

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 6)
    Corner.Parent = Frame

    local Title = Instance.new("TextLabel")
    Title.Parent = Frame
    Title.BackgroundTransparency = 1
    Title.Position = UDim2.new(0, 8, 0, 4)
    Title.Size = UDim2.new(0, 340, 0, 18)
    Title.Font = Enum.Font.GothamBold
    Title.Text = title
    Title.TextColor3 = Color3.fromRGB(0, 200, 255)
    Title.TextSize = 12
    Title.TextXAlignment = Enum.TextXAlignment.Left

    local Desc = Instance.new("TextLabel")
    Desc.Parent = Frame
    Desc.BackgroundTransparency = 1
    Desc.Position = UDim2.new(0, 8, 0, 22)
    Desc.Size = UDim2.new(0, 340, 0, 22)
    Desc.Font = Enum.Font.Gotham
    Desc.Text = content
    Desc.TextColor3 = Color3.fromRGB(220, 230, 255)
    Desc.TextSize = 10
    Desc.TextXAlignment = Enum.TextXAlignment.Left
    Desc.TextWrapped = true
end

local function AddButton(tab, name, callback)
    local Btn = Instance.new("TextButton")
    Btn.Parent = tab
    Btn.BackgroundColor3 = Color3.fromRGB(0, 120, 210)
    Btn.BackgroundTransparency = 0.2
    Btn.Size = UDim2.new(0, 355, 0, 32)
    Btn.Font = Enum.Font.GothamBold
    Btn.Text = name
    Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Btn.TextSize = 11

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 6)
    Corner.Parent = Btn

    Btn.MouseButton1Click:Connect(function()
        pcall(function()
            local ClickSound = Instance.new("Sound")
            ClickSound.SoundId = "rbxassetid://12221967"
            ClickSound.Volume = 1.0
            ClickSound.Parent = ProtectedGui
            ClickSound:Play()
        end)
        pcall(callback)
    end)
end

-- ================= TABS SETUP =================
local TabCreator = CreateTab("Creator")
AddParagraph(TabCreator, "Developer Info", "Owner: RENE BATERBONIA (JOHNBERT)\nUser ID: 8064284458")

-- ACCOUNTS TAB (TikTok, Facebook, Instagram)
local TabAccounts = CreateTab("Accounts")
AddParagraph(TabAccounts, "Social Media Links", "I-click aron ma-copy sa clipboard ang mga accounts.")
AddButton(TabAccounts, "Copy TikTok Account", function()
    if setclipboard then setclipboard("tiktok.com/@valenzonajohnbert") end
end)
AddButton(TabAccounts, "Copy Facebook Account", function()
    if setclipboard then setclipboard("https://www.facebook.com/profile.php?id=61584512375546") end
end)
AddButton(TabAccounts, "Copy Instagram Account", function()
    if setclipboard then setclipboard("https://www.instagram.com/valenzonajohnbert22") end
end)

local TabSettings = CreateTab("Settings")
AddParagraph(TabSettings, "Steal An Egg Admin Abuse", "Target Start Time: Alas 11:00 sa gabii (11:00 PM).")
AddButton(TabSettings, "Enable Potato Graphics", function()
    workspace.CurrentCamera.FieldOfView = 70
    Lighting.GlobalShadows = false
    Lighting.FogEnd = 9e9
    for _, v in ipairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic v.Reflectance = 0
        elseif v:IsA("Decal") or v:IsA("Texture") then v.Transparency = 1 end
    end
end)

-- Music Player Tab
local TabMusic = CreateTab("Music Player")
AddParagraph(TabMusic, "Custom Background Music", "Music ID: 113377354243758")
AddButton(TabMusic, "Replay Music", function()
    pcall(function()
        if _G.CurrentCustomMusic then _G.CurrentCustomMusic:Stop() _G.CurrentCustomMusic:Destroy() end
        local sound = Instance.new("Sound")
        sound.SoundId = "rbxassetid://113377354243758"
        sound.Volume = 1.0
        sound.Looped = false
        sound.Parent = ProtectedGui
        sound:Play()
        _G.CurrentCustomMusic = sound
    end)
end)
AddButton(TabMusic, "Stop Music", function()
    pcall(function()
        if _G.CurrentCustomMusic then _G.CurrentCustomMusic:Stop() end
    end)
end)

-- Roblox Animations Tab
local TabAnims = CreateTab("Roblox Anims")
AddParagraph(TabAnims, "Animation Packs", "E-execute ang tanan sikat nga Roblox animation packs.")

local function PlayAnimPack(idleId)
    pcall(function()
        local character = LocalPlayer.Character
        if not character then return end
        local animate = character:FindFirstChild("Animate")
        if animate then animate.Disabled = true end
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            for _, track in ipairs(humanoid:GetPlayingAnimationTracks()) do track:Stop() end
            local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
            local anim = Instance.new("Animation")
            anim.AnimationId = idleId
            local track = animator:LoadAnimation(anim)
            track:Play()
        end
    end)
end

AddButton(TabAnims, "Toy Animation Pack", function() PlayAnimPack("rbxassetid://507765000") end)
AddButton(TabAnims, "Zombie Animation Pack", function() PlayAnimPack("rbxassetid://616158929") end)
AddButton(TabAnims, "Mage Animation Pack", function() PlayAnimPack("rbxassetid://707742142") end)
AddButton(TabAnims, "Ninja Animation Pack", function() PlayAnimPack("rbxassetid://656117404") end)
AddButton(TabAnims, "Elder Animation Pack", function() PlayAnimPack("rbxassetid://845397899") end)
AddButton(TabAnims, "Viking Animation Pack", function() PlayAnimPack("rbxassetid://1049009941") end)
AddButton(TabAnims, "Levitation Animation Pack", function() PlayAnimPack("rbxassetid://1149215189") end)

-- STEAL AN EGG TAB (Gi-usa ra ang pinakamaayo nga script)
local TabStealEgg = CreateTab("Steal An Egg")
AddParagraph(TabStealEgg, "Steal An Egg Hub", "Automated farming, auto-collect, ug utility script.")
AddButton(TabStealEgg, "Execute Steal An Egg Script", function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.Image/lennonxscripts/lennonhubv3/refs/heads/main/stealanegg.lua"))()
    end)
end)
