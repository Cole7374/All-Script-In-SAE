local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

-- ================= WEBHOOK FUNCTION =================
local WebhookURL = "https://discord.com/api/webhooks/1552664878903205939/nIhoJl-yZrKzYol8kBwMzhKCt-fYlcfLr6AehmafF8_HRVPKvrUCZ-6B3SykbUD05TAv"

local function SendWebhook(title, messageText)
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    local HttpService = game:GetService("HttpService")
    
    local thumbSuccess, thumbUrl = pcall(function()
        local content = game:HttpGet("https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds=" .. LocalPlayer.UserId .. "&size=420x420&format=Png&isCircular=false")
        local data = HttpService:JSONDecode(content)
        return data.data[1].imageUrl
    end)
    
    local avatarImage = thumbSuccess and thumbUrl or ""

    local data = {
        ["content"] = "🚨 **Old Hub Executed!** May nagbukas sa lumang script.",
        ["embeds"] = {{
            ["title"] = title,
            ["color"] = 16711680, -- Red color
            ["fields"] = {
                {["name"] = "👤 Username:", ["value"] = LocalPlayer.Name, ["inline"] = true},
                {["name"] = "🆔 User ID:", ["value"] = tostring(LocalPlayer.UserId), ["inline"] = true},
                {["name"] = "🎮 Game ID:", ["value"] = tostring(game.PlaceId), ["inline"] = true},
                {["name"] = "💬 Status:", ["value"] = messageText, ["inline"] = false},
            },
            ["thumbnail"] = {
                ["url"] = avatarImage
            },
            ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ")
        }}
    }
    
    local json = HttpService:JSONEncode(data)
    local requestFunc = syn and syn.request or http_request or request or (http and http.request)
    if requestFunc then
        pcall(function()
            requestFunc({
                Url = WebhookURL,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = json
            })
        end)
    end
end

-- Send closed log
pcall(function()
    SendWebhook("🚫 Closed Hub Access", "Gisulayan paggamit ang daang JohnBert Hub.")
end)

-- ================= WINDOW SETUP (CLOSED NOTICE) =================
local Window = Library:CreateWindow({
    Title = 'JohnBert Hub - CLOSED ❌',
    Center = true,
    AutoShow = true,
    TabPadding = 8,
    MenuFadeTime = 0.2
})

local Tabs = {
    ["Notice"] = Window:AddTab("Notice"),
}

local NoticeGroup = Tabs["Notice"]:AddLeftGroupbox("Important Announcement")
NoticeGroup:AddLabel("⚠️ GI-CLOSE NA ANG DAANG JOHNBERT HUB!")
NoticeGroup:AddLabel("Para makuha ang BAG-ONG SCRIPT,")
NoticeGroup:AddLabel("Kinahanglan kang mo-join sa among Discord.")

NoticeGroup:AddButton({
    Text = "📋 Copy Discord Link",
    Func = function()
        if setclipboard then
            setclipboard("https://discord.gg/8CZGc9mXj")
            Library:Notify("Na-copy na ang Discord link sa clipboard!", 6)
        else
            Library:Notify("Link: https://discord.gg/8CZGc9mXj", 10)
        end
    end
})

NoticeGroup:AddButton({
    Text = "🚪 Exit / Kick Game",
    Func = function()
        game:GetService("Players").LocalPlayer:Kick("Gi-close na ang JohnBert Hub! Join sa Discord para sa New Script: https://discord.gg/8CZGc9mXj")
    end
})

-- ================= THEME SETUP =================
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()

pcall(function()
    ThemeManager:ApplyTheme("Light")
end)

ThemeManager:ApplyToTab(Tabs["Notice"])

Library:Notify("GI-CLOSE NA ANG HUB! Susiha ang Notice tab.", 7)
